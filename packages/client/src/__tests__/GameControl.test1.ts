/* import useGameControls from '../features/GameCanvas/model/hocs/useGameControls'

let events: Record<string, any> = [];

document.addEventListener = jest.fn((event, cb) => {
  events[event] = cb;
})

describe('GameControls', () => {
  it('should turn direction from direct to up', () {
    ;
  })
}) */
